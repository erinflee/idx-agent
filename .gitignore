# api keys & local variables
.env
*.env
# template (no secrets) — keep this one tracked
!.env.example

# ceo rule: no raw data or dumps in repo
*.csv
*.xlsx
*.xls
*.sql
*.json
!package.json
!package-lock.json
!baseline.json
# city lookup table the property-search parser depends on
!skills/propertySearch/cities.json

# week 6 embedding artifacts -> ~81MB, rebuild with python -m scripts.build_embeddings
*.npy
*.npz

# week 9 trained router artifact -> rebuild with python -m evals.router_model
*.joblib

# rag corpus -> licensed source pdfs + rebuildable generated mds 
rag_docs/*.pdf
rag_docs/*.md
rag_docs/*.jsonl

# node / typescript deps
node_modules/

# python builds / venv
__pycache__/
*.py[cod]
.venv/
venv/
env/

# mac / editor artifacts
.DS_Store
.vscode/
.idea/