<!-- Week 6 — OpenClaw skill manifest for semantic search.
     Frontmatter: name, description, user-invocable, metadata (emoji).
     The description is what routes the agent here instead of property-search —
     make it clear this one takes a vibe/description, not structured filters. -->
